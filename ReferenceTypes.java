import java.sql.Date;


/*primitive are numbers, boolean, characters,reference types are to store complex data types
  */
public class ReferenceTypes {
    public static void main(String[] args){
        /* For primitive memory allocation is not needed, for reference allocation is needed */
        Date now =new Date(); //now is the name of the variable,new is to allocated to set the variable
        System.out.println(age);

    }
}
